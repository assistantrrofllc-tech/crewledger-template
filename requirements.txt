# CrewLedger — Phase 1 Dependencies

# Web framework
flask>=3.0

# Twilio SMS/MMS
twilio>=9.0

# HTTP requests (downloading MMS images from Twilio)
requests>=2.31

# OpenAI Vision API (Step 3)
openai>=1.12

# Fuzzy string matching (Step 8)
thefuzz>=0.22
python-Levenshtein>=0.25

# Environment variable management
python-dotenv>=1.0

# Production WSGI server
gunicorn>=21.2

# Excel export
openpyxl>=3.1

# PDF processing (CrewCert cert splitter)
pdfplumber>=0.10
pypdf>=4.0

# QR code generation (CrewCert public verify)
qrcode[pil]>=7.4

# Background job scheduler (cert status refresh)
apscheduler>=3.10

# CSV export (Step 13) — stdlib, listed for clarity
# csv (built-in)
